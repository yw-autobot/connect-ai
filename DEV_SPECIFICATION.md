# 🚀 AI 콘텐츠 제작 플랫폼 (SaaS) 개발 명세서

## I. 개요 및 목표
*   **플랫폼 성격:** 단일 SaaS 작업 앱 형태의 AI 기반 영상/콘텐츠 제작 워크플로우 엔진.
*   **핵심 사용자 플로우:** '영상 분석'과 '대본 제작'이라는 두 가지 주력 경로를 중심으로 모든 기능이 연결되어야 함.
*   **주요 목표:** 사용자가 최소한의 개입으로 고품질 콘텐츠(특히 쇼핑/트렌드 기반)를 빠르고 효율적으로 생성하고 수익화할 수 있도록 지원하는 것.

## II. 사용자 인터페이스 (UI/UX 구조)
### 1. 기본 레이아웃 (Critical Structure)
*   **좌측 사이드바:** 프로젝트 관리, 트렌드 리서치, 채널 & 콘텐츠, 내 도구 등의 주요 모듈 링크 포함.
*   **상단 바:** 멤버십/기수 표시, **포인트 잔액(P)** 표시, 충전, 테마 전환, 새로고침, 사용자 프로필 등 상시 노출 요소 배치.
*   **프로젝트 관리:** 현재 사용량/한도를 명확하게 "새 프로젝트 (1/10)" 형태로 표시.

### 2. 핵심 워크플로우 플로우 (Flow)
*   **A. 영상 분석 및 제작 (7단계 플로우):**
    1.  **영상 분석 입력:** 단일 영상(URL/파일 업로드), 여러 영상 믹스, 파일 업로드를 지원하며 최대 크기 500MB 제한.
    2.  **작업 상태 관리:** '분석 중' 시 새로고침 및 중복 제출 불가 처리 (Job ID 기반). 진행률(%), 경과 시간(elapsedSec), 예상 총 시간(estimatedTotalSec), 남은 시간(remainingSec) 표시 필수.
    3.  **단계별 작업 흐름:** 영상 분석 → 영상 선택 → 스크립트 & 음성 생성 → 스타일 설정 → 음성 프리뷰 → SEO 최적화 → 내보내기.
*   **B. 대본 제작 플로우:** 자유 스크립트 모드와 장면 맞춤(Scene-by-scene) 모드를 지원하며, 초기 상태는 '준비중/비활성'으로 설정 가능.

## III. 핵심 기능별 요구사항 및 데이터 모델링 (Models & APIs)
### 1. 콘텐츠 큐레이션 및 검색 (Discovery)
*   **[21] 신규 쇼핑 영상:** 관리자 전용 페이지. 카드형 목록(썸네일, 제목, 채널명, 날짜, N명이 담아감). `CuratedVideo` 모델 사용.
*   **[22] 추천 픽 영상:** 선착순/제한 수량 구조. `RecommendedPick` 모델 사용. (`capacity`, `savedCount`, `isClosed` 필드). '담기' 성공 시 원자적(Atomic) 카운트 증가 및 중복 방지 처리 필수.
*   **[23] 내 모니터링 리스트:** 플랫폼별 그룹 관리. `MonitoringChannel` 모델 사용. (YouTube, IG, TikTok 등). 신규 영상/쇼핑 후보 감지 알림 로직 필요.
*   **[24] 쇼핑 링크 프로필 (ShopLinkProfile):** 공개 URL `/s/{slug}` 구조 구현. 이미지 업로드(프로필/커버), SNS 링크 저장, 쿠팡 파트너스 ID 저장 기능 필수.

### 2. 영상 분석 및 도구 (Tooling & Utility)
*   **[Video Downloader]:** 샤오홍슈, TikTok, IG, Shorts 등 지원. **0.5P 차감**, 실패 시 자동 환불 로직(`DownloadJob` 모델 사용).
*   **[Style Profile Library]:** 영상 URL 분석을 통한 스타일 추출 및 저장 (`StyleProfile` 모델). 공개/비공개 목록화, 라이브러리 복사 기능 제공.

### 3. 수익화 및 결제 시스템 (Monetization & Billing)
*   **[Point Ledger]:** 모든 포인트 변화 기록의 원칙적 사용. `PointLedger` 모델 필수 구현.
    *   필드: id, userId, type(charge/spend/refund/admin\_adjust), amount, balanceAfter, reason, relatedJobId, relatedProjectId, createdAt.
    *   로직: 트랜잭션 기반의 포인트 차감 및 환불 처리 로직 구현.

## IV. MVP 우선순위 (Critical Path)
1.  인증/세션/보호 라우트 및 기본 레이아웃 구성.
2.  프로젝트 생성 한도 관리 및 프로젝트 리스트 구현.
3.  **영상 분석 7단계 플로우 UI** (최대 복잡성).
4.  URL/파일 입력 기반 **분석 작업 큐** 시스템 구축 (비동기 처리).
5.  스타일 프로필 라이브러리 조회 및 저장 기능.
6.  PointLedger를 이용한 다운로드 도구 (0.5P 과금) 구현.
7.  내 쇼핑 링크 공개 프로필 (`/s/{slug}`) 생성.